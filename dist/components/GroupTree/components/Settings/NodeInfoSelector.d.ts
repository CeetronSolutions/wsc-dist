import React from "react";
import { DataInfos } from "../../redux/types";
interface Props {
    node_options: DataInfos;
}
declare const NodeInfoSelector: React.FC<Props>;
export default NodeInfoSelector;
